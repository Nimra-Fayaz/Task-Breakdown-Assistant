# AI Service Configuration
# Choose: "ollama" (local, free - RECOMMENDED), "gemini" (free tier), or "openai" (paid)
AI_SERVICE=ollama

# Ollama Configuration (Local, FREE - Recommended)
# 1. Install from: https://ollama.com
# 2. Close and reopen PowerShell after installation
# 3. Run: ollama pull llama3.2
# 4. Verify: ollama list (should show llama3.2)
# Model options: llama3.2, llama3.1, mistral, etc.
OLLAMA_MODEL=llama3.2

# Google Gemini API (Free tier - requires API key)
# Get free API key from: https://aistudio.google.com/app/apikey
# Only needed if AI_SERVICE=gemini
GEMINI_API_KEY=your_gemini_api_key_here

# OpenAI API (Paid)
# Only needed if AI_SERVICE=openai
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration
DATABASE_URL=sqlite:///./task_breakdown.db

# Server Configuration
HOST=0.0.0.0
PORT=8000
